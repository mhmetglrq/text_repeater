import 'dart:ui';

class L10n {
  L10n._();

  static final supportedLocales = [
    const Locale("en"), // İngilizce
    const Locale("zh"), // Çince (Mandarin)
    const Locale("es"), // İspanyolca
    const Locale("hi"), // Hintçe
    const Locale("ar"), // Arapça
    const Locale("pt"), // Portekizce
    const Locale("fr"), // Fransızca
    const Locale("id"), // Endonezce
    const Locale("tr"), // Türkçe
  ];
}
